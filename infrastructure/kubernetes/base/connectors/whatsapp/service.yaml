apiVersion: v1
kind: Service
metadata:
  name: whatsapp-connector
  labels:
    app: whatsapp-connector
    component: connector
spec:
  type: ClusterIP
  selector:
    app: whatsapp-connector
  ports:
  - name: http
    port: 8091
    targetPort: 8091
    protocol: TCP
