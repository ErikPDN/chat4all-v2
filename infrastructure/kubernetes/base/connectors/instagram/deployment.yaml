apiVersion: apps/v1
kind: Deployment
metadata:
  name: instagram-connector
  labels:
    app: instagram-connector
    component: connector
    tier: integration
spec:
  replicas: 1
  selector:
    matchLabels:
      app: instagram-connector
  template:
    metadata:
      labels:
        app: instagram-connector
        component: connector
        tier: integration
    spec:
      containers:
      - name: instagram-connector
        image: chat4all/instagram-connector:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8093
          name: http
          protocol: TCP
        envFrom:
        - configMapRef:
            name: instagram-connector-config
        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8093
          initialDelaySeconds: 60
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8093
          initialDelaySeconds: 30
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        resources:
          requests:
            memory: "384Mi"
            cpu: "200m"
          limits:
            memory: "768Mi"
            cpu: "400m"
