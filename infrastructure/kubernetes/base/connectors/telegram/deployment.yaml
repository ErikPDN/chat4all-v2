apiVersion: apps/v1
kind: Deployment
metadata:
  name: telegram-connector
  labels:
    app: telegram-connector
    component: connector
    tier: integration
spec:
  replicas: 1
  selector:
    matchLabels:
      app: telegram-connector
  template:
    metadata:
      labels:
        app: telegram-connector
        component: connector
        tier: integration
    spec:
      containers:
      - name: telegram-connector
        image: chat4all/telegram-connector:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8092
          name: http
          protocol: TCP
        envFrom:
        - configMapRef:
            name: telegram-connector-config
        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8092
          initialDelaySeconds: 60
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8092
          initialDelaySeconds: 30
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        resources:
          requests:
            memory: "384Mi"
            cpu: "200m"
          limits:
            memory: "768Mi"
            cpu: "400m"
